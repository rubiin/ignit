version: 2

project_name: ignit

before:
  hooks:
    # Generate shell completion scripts once, ahead of all builds, so they
    # can be bundled into archives and installed by the AUR package.
    - mkdir -p .buildcache/completions
    - sh -c 'go run . completion bash > .buildcache/completions/ignit.bash'
    - sh -c 'go run . completion zsh > .buildcache/completions/_ignit'
    - sh -c 'go run . completion fish > .buildcache/completions/ignit.fish'
    - sh -c 'go run . completion pwsh > .buildcache/completions/_ignit.ps1'

builds:
  - env:
      - CGO_ENABLED=0
    # main.go declares `version`; release builds override it here.
    ldflags:
      - -s -w
      - -X main.version={{.Version}}
    goos:
      - linux
      - windows
      - darwin



archives:
  - formats: [tar.gz]
    # this name template makes the OS and Arch compatible with the results of `uname`.
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    # use zip for windows archives
    format_overrides:
      - goos: windows
        formats: [zip]
    files:
      - LICENSE
      # Bundle the generated completions as a completions/ folder in the archive.
      - src: .buildcache/completions/*
        dst: completions
        strip_parent: true

changelog:
  sort: desc
  use: git

  groups:
    - title: 🚀 New Features
      regexp: '^.*?feat(\([[:word:]]+\))??!?:.+$'
      order: 0
    - title: "🐛 Bug Fixes"
      regexp: '^.*?bug(\([[:word:]]+\))??!?:.+$'
      order: 1
    - title: Others
      order: 999


aurs:
  - name: ignit-bin
    homepage: https://github.com/rubiin/ignit
    description: Quickly generate .gitignore files for your projects from a curated list of environment templates.
    license: GPL-3.0-only
    maintainers:
      - "rubiin <roobin.bhandari@gmail.com>"
    git_url: ssh://aur@aur.archlinux.org/ignit-bin.git
    private_key: "{{ .Env.AUR_KEY }}"
    provides:
      - ignit
    conflicts:
      - ignit
      - ignit-git
    package: |-
      # bin
      install -Dm755 "./ignit" "${pkgdir}/usr/bin/ignit"
      # license
      install -Dm644 "./LICENSE" "${pkgdir}/usr/share/licenses/ignit/LICENSE"
      # shell completions
      install -Dm644 "./completions/ignit.bash" "${pkgdir}/usr/share/bash-completion/completions/ignit"
      install -Dm644 "./completions/_ignit" "${pkgdir}/usr/share/zsh/site-functions/_ignit"
      install -Dm644 "./completions/ignit.fish" "${pkgdir}/usr/share/fish/vendor_completions.d/ignit.fish"

release:
  footer: >-

    ---

    Released by [GoReleaser](https://github.com/goreleaser/goreleaser).
